{
  "name": "bloodlink",
  "version": "1.0.0",
  "private": true,
  "description": "BloodLink - rastreabilidade de bolsas de sangue com blockchain (projeto acadêmico)",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix blockchain",
    "blockchain:start": "npm run node --prefix blockchain",
    "blockchain:deploy": "npm run deploy --prefix blockchain",
    "blockchain:compile": "npm run compile --prefix blockchain",
    "blockchain:test": "npm run test --prefix blockchain",
    "db:migrate": "npm run db:migrate --prefix backend",
    "db:generate": "npm run db:generate --prefix backend",
    "db:seed": "npm run db:seed --prefix backend",
    "backend:dev": "npm run dev --prefix backend",
    "backend:test": "npm run test --prefix backend",
    "frontend:dev": "npm run dev --prefix frontend",
    "frontend:test": "npm run test --prefix frontend",
    "test:all": "npm run backend:test && npm run frontend:test && npm run blockchain:test"
  }
}
